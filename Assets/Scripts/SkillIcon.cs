using UnityEngine;
using UnityEngine.UI;

public class SkillIcon : MonoBehaviour
{
    public Skill skill;  // Skilli, joka liittyy tähän ikoniin

    void Start()
    {
        // Lisää tänne koodi, joka alustaa ikonin (esim. asettaa skillin nimen ja ikonin)
    }
}
